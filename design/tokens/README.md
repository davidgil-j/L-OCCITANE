# design/tokens/

`colors.json` y `typography.json` son la única fuente de verdad de marca.
Cuando se actualicen con los valores reales (extracción con SkillUI o
materiales de Yasmina), hay que reflejar los mismos valores en
`tailwind.config.js`.
