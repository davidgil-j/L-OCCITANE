# components/animations/

Wrappers de Framer Motion reutilizables (p. ej. `FadeIn`). Un único
momento de animación bien orquestado por sección — evitar animar cada
tarjeta/elemento por separado (es uno de los "tells" más comunes de una
página generada por IA).
