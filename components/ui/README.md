# components/ui/

Componentes atómicos sin lógica de negocio ni copy embebido: reciben todo
por props. Ejemplo: `Button.jsx`. Si un componente empieza a contener
texto o datos específicos de L'Occitane, no pertenece aquí — va en
`sections/`.
