# components/sections/

Bloques completos de la landing, uno por sección de página
(`Hero`, `ProductShowcase`, `CTA`, `Footer`). Reciben el contenido como
props desde `content/loccitane.js` — no deben tener textos hardcodeados.
